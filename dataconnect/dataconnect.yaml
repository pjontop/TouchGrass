specVersion: "v1"
serviceId: "touchgrass"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "touchgrass-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
